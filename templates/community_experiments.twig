<div class="container">
    <div class="header">
        <h1>Community Experiments</h1>
    </div>
    <div class="content">
        <ul>
            {% for experiment in experiments %}
                <li>{{ experiment.name }}</li>
            {% endfor %}
        </ul>
    </div>
</div>

